<UserControl x:Class="VideoMonitor.Wpf.Controls.MonitorTree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Background="{StaticResource PanelBackgroundBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1,0,0,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="62" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Border BorderBrush="{StaticResource SeparatorBrush}" BorderThickness="0,0,0,1">
                <Grid Margin="18,0,14,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Style="{StaticResource SectionTitleTextStyle}" Text="监控设备" />
                        <TextBlock Margin="0,3,0,0" Style="{StaticResource MutedTextStyle}" Text="设备组与固定业务视图" />
                    </StackPanel>
                    <Border Grid.Column="1"
                            Width="28"
                            Height="22"
                            CornerRadius="4"
                            Background="{StaticResource PrimaryBlueMutedBrush}">
                        <TextBlock HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Foreground="{StaticResource PrimaryBlueBrush}"
                                   FontSize="11"
                                   Text="12" />
                    </Border>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <ItemsControl Margin="10,12" ItemsSource="{Binding TreeSections}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Expander Margin="0,0,0,6" IsExpanded="True">
                                <Expander.Header>
                                    <StackPanel Margin="4,7" Orientation="Horizontal">
                                        <Ellipse Width="6" Height="6" Margin="0,0,9,0" Fill="{StaticResource PrimaryBlueBrush}" />
                                        <TextBlock Foreground="{StaticResource PrimaryTextBrush}"
                                                   FontSize="13"
                                                   FontWeight="SemiBold"
                                                   Text="{Binding Name}" />
                                    </StackPanel>
                                </Expander.Header>
                                <ItemsControl Margin="15,2,0,6" ItemsSource="{Binding Children}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Button Height="34"
                                                    Margin="0,1"
                                                    Padding="11,0"
                                                    HorizontalContentAlignment="Left"
                                                    Background="Transparent"
                                                    BorderThickness="0"
                                                    Command="{Binding DataContext.SelectGroupCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                                                    CommandParameter="{Binding}"
                                                    Cursor="Hand">
                                                <StackPanel Orientation="Horizontal">
                                                    <Ellipse Width="6" Height="6" Margin="0,0,9,0" Fill="{StaticResource OnlineGreenBrush}" />
                                                    <TextBlock VerticalAlignment="Center"
                                                               Foreground="{StaticResource SecondaryTextBrush}"
                                                               Text="{Binding Name}" />
                                                </StackPanel>
                                            </Button>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Expander>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
